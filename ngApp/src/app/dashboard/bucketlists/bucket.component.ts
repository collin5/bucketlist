import { Component } from '@angular/core';

@Component({
    selector: 'app-bucket',
    moduleId: module.id,
    templateUrl: 'bucket.component.html'
})
export class Bucket{
    title: string = "Bucket";
    description: string = "description"
}
